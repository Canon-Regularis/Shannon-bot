name: Release

# Called by CI once its checks have passed, rather than triggered by the push itself. Triggering
# on the push would race the test matrix on main and skip it altogether on a tag, so a tagged
# commit could publish a signed, attested image having run nothing. Being called rather than
# triggered also keeps github.ref pointing at the tag, which is what the tagging below reads.
on:
  workflow_call:
  workflow_dispatch:

permissions:
  contents: read

# Never cancel a publish half way through.
concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

env:
  REGISTRY: ghcr.io
  IMAGE: ${{ github.repository }}

jobs:
  publish:
    name: Publish image
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
      packages: write
      # Needed to sign the build provenance.
      id-token: write
      attestations: write

    steps:
      - uses: actions/checkout@v7

      - uses: docker/setup-qemu-action@v4
        if: startsWith(github.ref, 'refs/tags/')

      - uses: docker/setup-buildx-action@v4

      - uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Work out the tags
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE }}
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern={{major}},enable=${{ !startsWith(github.ref, 'refs/tags/v0.') }}
            type=raw,value=edge,enable=${{ github.ref == 'refs/heads/main' }}
            # What the server follows, and the only tag here that means somebody decided. `edge`
            # moves on every green push to main, which is the right thing for a tag nobody
            # deploys from and the wrong thing for one a timer watches: a merge on a Friday
            # evening would restart a live bot with nobody looking at it. Moving this one is a
            # `git tag v0.6.0 && git push --tags`, which is a deliberate act, recorded in git,
            # and already the thing this project writes a CHANGELOG entry for.
            type=raw,value=stable,enable=${{ startsWith(github.ref, 'refs/tags/v') }}
            # The immutable one. Rollback pins this: `sha-<full commit>` always names one build.
            type=sha,format=long
          labels: |
            org.opencontainers.image.title=Shannon Bot
            org.opencontainers.image.description=Syncs GitHub repository activity into Discord

      - name: Build and push
        id: push
        uses: docker/build-push-action@v7
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          # What /health reports back. github.sha is ambient in a called workflow too, so
          # neither this workflow nor the one calling it needs an input for it.
          build-args: SHANNON_BUILD=${{ github.sha }}
          # arm64 goes through QEMU and is slow, so it is only worth paying for on a real
          # release. Pushes to main stay amd64 and fast.
          platforms: >-
            ${{ startsWith(github.ref, 'refs/tags/')
                && 'linux/amd64,linux/arm64' || 'linux/amd64' }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          provenance: mode=max
          sbom: true

      - name: Attest the build
        uses: actions/attest-build-provenance@v4
        with:
          subject-name: ${{ env.REGISTRY }}/${{ env.IMAGE }}
          subject-digest: ${{ steps.push.outputs.digest }}
          push-to-registry: true

      - name: Summary
        run: |
          {
            echo "### Published"
            echo
            echo '```'
            echo "${{ steps.meta.outputs.tags }}"
            echo '```'
            echo
            echo "Digest: \`${{ steps.push.outputs.digest }}\`"
          } >> "$GITHUB_STEP_SUMMARY"
